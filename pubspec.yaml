name: traderedge_fx_app
description: TraderEdge FX App
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.0.0
  firebase_auth: ^4.0.0
  cloud_firestore: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo/
    - assets/icons/
